<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.Text?>

<StackPane xmlns="http://javafx.com/javafx"
           xmlns:fx="http://javafx.com/fxml"
           fx:controller="com.care.controller.shared.RegisterController"
           styleClass="root"
           prefHeight="900" prefWidth="1400">
    
    <!-- Centered Register Card -->
    <VBox alignment="CENTER" spacing="15" styleClass="login-container">
        <padding>
            <Insets top="35" right="60" bottom="35" left="60"/>
        </padding>
        
        <!-- Title Section -->
        <VBox alignment="CENTER" spacing="5">
            <Text text="Create Account" styleClass="title" style="-fx-font-size: 32px;"/>
            <Text text="Join CARE to get instant support" styleClass="subtitle" style="-fx-font-size: 13px;"/>
        </VBox>
        
        <!-- Registration Form -->
        <VBox spacing="12" styleClass="form-container">
            
            <!-- Full Name -->
            <VBox spacing="5">
                <Label text="Full Name" styleClass="form-label" style="-fx-font-size: 12px;"/>
                <TextField fx:id="nameField" 
                          promptText="John Doe" 
                          styleClass="form-field"
                          prefHeight="40"/>
            </VBox>
            
            <!-- Email -->
            <VBox spacing="5">
                <Label text="Email Address" styleClass="form-label" style="-fx-font-size: 12px;"/>
                <TextField fx:id="emailField" 
                          promptText="your.email@example.com" 
                          styleClass="form-field"
                          prefHeight="40"/>
            </VBox>
            
            <!-- Password -->
            <VBox spacing="5">
                <Label text="Password" styleClass="form-label" style="-fx-font-size: 12px;"/>
                <PasswordField fx:id="passwordField" 
                              promptText="Minimum 6 characters" 
                              styleClass="form-field"
                              prefHeight="40"/>
            </VBox>
            
            <!-- Confirm Password -->
            <VBox spacing="5">
                <Label text="Confirm Password" styleClass="form-label" style="-fx-font-size: 12px;"/>
                <PasswordField fx:id="confirmPasswordField" 
                              promptText="Re-enter your password" 
                              styleClass="form-field"
                              prefHeight="40"/>
            </VBox>
            
            <!-- License Key (Optional) -->
            <VBox spacing="5">
                <Label text="License Key (Optional)" styleClass="form-label" style="-fx-font-size: 12px;"/>
                <TextField fx:id="licenseKeyField" 
                          promptText="Enter if you have a license key" 
                          styleClass="form-field"
                          prefHeight="40"/>
            </VBox>
            
            <!-- Error Message -->
            <Label fx:id="errorLabel" 
                   styleClass="error-label" 
                   visible="false"
                   wrapText="true"/>
            
            <!-- Spacer -->
            <Region minHeight="3"/>
            
            <!-- Buttons -->
            <VBox spacing="10" alignment="CENTER">
                <Button fx:id="registerButton" 
                       text="Create Account" 
                       onAction="#handleRegister" 
                       styleClass="primary-button"
                       maxWidth="Infinity"
                       prefHeight="42"/>
                
                <!-- Separator -->
                <HBox alignment="CENTER" spacing="10">
                    <Separator HBox.hgrow="ALWAYS"/>
                    <Label text="or" style="-fx-text-fill: #adb5bd; -fx-font-size: 11px;"/>
                    <Separator HBox.hgrow="ALWAYS"/>
                </HBox>
                
                <Button fx:id="backButton" 
                       text="Back to Login" 
                       onAction="#handleBack" 
                       styleClass="secondary-button"
                       maxWidth="Infinity"
                       prefHeight="42"/>
            </VBox>
            
        </VBox>
        
        <!-- Privacy Note -->
        <Label text="By creating an account, you agree to CARE's Terms of Service" 
               style="-fx-text-fill: #adb5bd; -fx-font-size: 10px; -fx-padding: 10 0 0 0;"
               wrapText="true"/>
        
    </VBox>
    
</StackPane>

